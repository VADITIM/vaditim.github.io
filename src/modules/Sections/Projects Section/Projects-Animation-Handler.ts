/**
 * Projects section animations.
 *
 * The Projects section was rebuilt as a fanned card feed (see
 * `aProjects-Section.vue`). Like the Playground, it co-locates its layout, drag
 * interactions, and enter/leave reveal inside the component using template refs
 * and `onSectionStatesChange`, so there is nothing to register here.
 *
 * This stub keeps the section consistent with the registry contract
 * (`registerAnimations`).
 */
export function registerProjectsAnimations() {
  // Intentionally empty — see component for the self-managed fan feed reveal.
}
