/**
 * Classified section animations; the reveal/exit are co-located inside
 * Classified-Section.vue (locked-state swap needs template refs). This stub
 * keeps the section consistent with the registry contract (`registerAnimations`).
 */
export function registerClassifiedAnimations() {
  // Intentionally empty; see component for the self-managed reveal.
}
